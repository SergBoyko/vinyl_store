<!DOCTYPE html>
<html>
<head>
  <title><%= content_for(:title) || "Vinyl Showcase" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <link rel="icon" href="/icon.png" type="image/png">
  <link rel="icon" href="/icon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/icon.png">
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class="bg-gray-100 text-gray-900 flex flex-col min-h-screen">

<!-- Хэдер -->
<header class="bg-gray-800 text-white py-4 shadow fixed top-0 left-0 w-full z-50">
  <div class="container mx-auto flex justify-between items-center px-5">
    <a href="/" class="text-2xl font-bold tracking-wide hover:text-gray-300">Vinyl Showcase</a>
    <nav class="flex space-x-4">
      <a href="/" class="text-gray-300 hover:text-white transition">Главная</a>
      <a href="/about" class="text-gray-300 hover:text-white transition">О нас</a>
      <a href="/contact" class="text-gray-300 hover:text-white transition">Контакты</a>
    </nav>
    <form action="/discs/search" method="get" class="relative">
      <input
        type="text"
        name="q"
        placeholder="Поиск..."
        class="bg-gray-700 text-white rounded-full px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500"
        value="<%= params[:q] %>">
      <button type="submit" class="absolute right-2 top-2 text-white">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M12.9 14.32a8 8 0 111.42-1.42l4.29 4.3a1 1 0 01-1.42 1.41l-4.3-4.29zm-5.9-2.82a6 6 0 100-12 6 6 0 000 12z" clip-rule="evenodd" />
        </svg>
      </button>
    </form>
  </div>
</header>

<!-- Основной контент -->
<main class="container mx-auto mt-28 px-5 flex-grow">
  <%= yield %>
</main>

<!-- Футер -->
<footer class="bg-gray-900 text-gray-300 py-6">
  <div class="container mx-auto text-center px-4">
    <p class="text-sm">&copy; <%= Date.today.year %> Vinyl Showcase. Все права защищены.</p>
  </div>
</footer>

</body>
</html>